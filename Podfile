# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

target 'New Smart' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for New Smart
  # Removed smart-view-sdk due to sandbox permission issues
  # Using manual integration instead

end
